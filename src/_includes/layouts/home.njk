{% extends "layouts/base.njk" %}
{% block headContent %}
  {# https://developers.google.com/search/docs/appearance/site-names#guidelines #}
  <script type="application/ld+json">
      {
        "@context" : "https://schema.org",
        "@type" : "WebSite",
        "name" : "{{ site.title }}",
        "url" : "{{ site.url }}"
      }
  </script>
  {# Add client spinner only to homepage, defer until document has been parsed as it relies on DOM elements #}
  <script defer src="/js/accordion-expand.js"></script>
{% endblock %}

{% block bodyContent %}
  <main class="home">
    <section class="hero">
      <h1>I design products that get us closer to net zero.</h1>
    </section>
    <section class="intro">
    <div class="recently">
    <input type="checkbox" id="expand-toggle" />
      <div class="expandable-content">
        {{ content | safe }}
      </div>
      <label for="expand-toggle" class="expand-btn small-text" tabindex="0">More</label>
    </div>
    <div class="right-now">
          <p><strong>Right now</strong> I’m running my freelance design practice. I’m also exploring how to lend my skills to alternate food production and food waste reduction ideas trying to survive the ‘valley of death’. Think circular economy projects like textiles or edible cutlery made from diverted (food) waste materials.</p>
          <p>Please {% include "partials/email-reach-out.html" %} if you have ideas or just want to say hello.</p>
    </div>
    </section>
    
  <section class="notes">
      <div class="header">
        <h2><a href="/notes">Notes</a></h2>
        <a class="rss" href="/feed.xml">RSS</a>
      </div>
      <div class="x-scroll-wrapper">
      <ul>
      {% for item in collections.note | reverse %}
      {# Limit to three items #}
       {% if loop.index0 < 3 %} 
      <li>
      {% if item.data.draft %} 
        <a href="#/">
        {% else %}
        <a href="{{ item.url }}">
        {% endif %}
          <h3>{{ item.data.title }}</h3>
          <h4>{{ item.data.tags[1] }}</h4>
        </a>
      </li>
      {% endif %}
      {% endfor %}
      <li><a href="/notes" class="small-text">More</a></li>
      </ul>
      </div>
  </section>
      

  <section class="projects">
    <h2>Misc</h2>
    <ul>
    {% for project in projects %}
    <li>
    <p>
    <a href="{{ project.url }}">
    <strong>{{ project.name }}</strong> {{ project.desc }}
    </a>
    </p>
    </li>
    {% endfor %}
    </ul>
  </section>

   
  </main>
{% endblock %}
